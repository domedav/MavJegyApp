# Agents

## Build

Kötelezően `build.sh`-t használni a buildelésre:

```bash
bash build.sh assembleDebug
```

`gradlew` nincs a repóban. A `build.sh` biztosítja a proxy-t, SDK-t, Gradle-t. Csak Termux + internet kell.
